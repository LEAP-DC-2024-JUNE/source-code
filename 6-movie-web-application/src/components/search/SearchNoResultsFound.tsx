"use client";

import { Card } from "../ui";

export const SearchNoResultsFound = () => {
  return <Card className="search-result flex items-center justify-center h-[95px]">No results found.</Card>;
};
